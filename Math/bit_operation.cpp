
void basic(){
    //フラグ管理
    // bit & (1<<i) iが立っているか
    // bit |= (1<<i) iを立てる
    // bit &= ~(1<<i) iを消す
    // _builtin_popcount(bit) 1の個数
    // bit | (1<<i) iを立てたもの
    // bit & ~(1<<i) iを消したもの
    // マスクビットなどで応用　集合をbitで持っておいて全探索
    // x&(-x) lowest bit
}



